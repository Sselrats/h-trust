공통

Step 1.
   현재 배치 가로로 배열 -> 새로로 배열하고, 설명을 1줄로 만들기. (ppt에서 Temporal RAG 느낌으로 보기 좋게 구성)

Step 2.
   Claim text 추가 (가능하면 키보드로 입력하고 있는 느낌이 나도 좋을듯)
   case 1에 경우 너는 사기꾼이란 표현을 쓸건가? << 쓰면 안되는 듯. M-GNN으로 탐지했으면 따로 기업 차원에서 조사 등을 신청해야 하지 않나 싶음. 법적 근거로는 활용이 X
   

Step 3.
   Domain findings Json 형식으로 변경 (색도 검은색으로 ppt와 통일?) << 좋은 듯
   여기서 그라데이션 넣지 말고, agent part에 그라데이션 넣으면 좋을 듯 함.
   json 형태로 하는 게 좋은가?

Step 4.
   D&C 컬러인 옅은 파란색으로 변경 or 초록
   내부 자료라는 표현이 들어가면 좋으려나?
   현재 output의 단점을 짚어주는게 나은가?

Step 5.
   Red team 빨간색으로 변경 or 초록
   내부 자료라는 표현이 들어가면 좋으려나?
   단점을 요약 정리한 줄이 들어가면 좋은가?

* step 4, 5를 합쳐가지고 agent step으로 하고, 두 agent의 배치를 위아래로 하는 게 어떤지?
debate 임을 표현하려면 step을 융합하는 게 좋을 거 같음
실제로도 이 부분은 자동화니까

Step 6.
   Human review -> Human step / 색 검은색으로 변경 (차콜 느낌?)
   내부 자료라는 표현이 들어가면 좋으려나? << db랑 interaction으로 표현하면 좋을 듯.

Step 7.
   Customer delivery -> Final Message / 초록색으로 변경
   safety 등 만족했음을 표현해주는 게 어떤지?
   질문도 같이 다시 명시해주면 좋을 듯함.